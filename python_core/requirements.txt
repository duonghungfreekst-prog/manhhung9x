# ========================================================
# DMH_Tools - Nội Soi AI 4K - Python Core Requirements
# Cài đặt: pip install -r requirements.txt
# ========================================================

# --- Core ---
numpy>=1.24.0
opencv-python>=4.8.0

# --- AI / Deep Learning (CUDA) ---
# Cài PyTorch với CUDA 11.8: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Cài PyTorch với CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# Cài PyTorch CPU only (không có GPU): pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# --- Audio ---
sounddevice>=0.4.6
scipy>=1.11.0       # FFT phân tích tần số

# --- Input / Trigger ---
pynput>=1.7.6       # Bàn đạp / Phím tắt

# --- Serial / COM Port ---
pyserial>=3.5

# --- Database ---
# sqlite3 tích hợp sẵn trong Python, không cần cài thêm

# --- Báo cáo ---
python-docx>=1.1.0
reportlab>=4.0.0    # Xuất PDF (tùy chọn)
Pillow>=10.0.0      # Xử lý ảnh cho Word/PDF

# --- WebSocket Server (stream Liveview → Electron/React) ---
websockets>=12.0
asyncio-mqtt>=0.16.1   # tùy chọn, dùng MQTT thay thế

# --- Tiện ích ---
psutil>=5.9.0       # Theo dõi RAM/GPU usage
